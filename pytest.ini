[pytest]
# Ohne diese Zeile warnt pytest bei jedem Lauf ueber den unbekannten
# Marker. Die Postgres-Tests ueberspringen sich selbst, wenn keine
# Datenbank erreichbar ist - die bestehende Suite laeuft also weiter
# ohne Docker (Auftrag Dafina, 08.09.2026, Phase 3).
markers =
    postgres: braucht ein laufendes Postgres (deploy/docker-compose.postgres.yml)
